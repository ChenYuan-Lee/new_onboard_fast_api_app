type Query {

    listings_check(
        listing_ids: [Int!]!
    ): [ListingsCheck!]

}

type ListingsCheck {
    listing_id: Int!
    bedrooms: Int!
    bathrooms: Float!
    price_breakdown: PriceBreakdown!
}

type PriceBreakdown {
    dollars: Float!
}
